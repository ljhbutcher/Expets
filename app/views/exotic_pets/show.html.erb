<div class="container">
  <!--<div class="image-section">
    <div class="main-image">
      <img src="<%= @exotic_pet.image_url %>" alt="<%= @exotic_pet.name %>" />
    </div>
    <div class="additional-images">
      <img src="<%= @exotic_pet.image_url %>" alt="<%= @exotic_pet.name %> Image 1" />
      <img src="<%= @exotic_pet.image_url %>" alt="<%= @exotic_pet.name %> Image 2" />
    </div>
  </div>-->
  <h3><%= @exotic_pet.name.upcase %></h3>

  <%= cl_image_tag(@exotic_pet.photo.key,width: 400, height: 400) %>

  <div class="details-section">

    <p class="rating">★★★★★</p>
    <p class="description">
      <%= @exotic_pet.description %>
    </p>
    <p class="age">Age: <%= @exotic_pet.age %> years</p>
    <button class="book-button">BOOK</button>
  </div>

  <div class="reviews-section">
    <h3>REVIEWS</h3>
    <p>Customer reviews</p>
  </div>
</div>

<div style="width: 400px; height: 400px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAP_BOX_API_KEY'] %>"></div>
