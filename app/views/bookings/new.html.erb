<div class="container-bookings">
   <div class="row justify-content-center">
      <div class="col-md-6">
         <h2>Book: <%= @exotic_pet.name %></h2>
         <%= simple_form_for [@exotic_pet, @booking] do |f| %>
            <!--<div class="form-group">
               <%= f.input :start_date, as: :date, label: "Start Date" %>
            </div>-->

            <div class="form-group">
               <%= f.input :start_date,
                           as: :string,
                           input_html: { data: { controller: "datepicker" },
                           placeholder: "📅 Select start date"  },
                           label: "Start Date" %>
            </div>
            <div class="form-group">
               <%= f.input :end_date,
                           as: :string,
                           input_html: { data: { controller: "datepicker" },
                           placeholder: "📅 Select end date"  },
                           label: "End Date" %>
            </div>


            <div class="form-group">
               <%= f.input :event_details, as: :text, label: "Event Details" %>
            </div>

            <div class="form-group">
               <%= f.input :contact_email, as: :email, label: "📧 Contact Email" %>
            </div>

            <div class="form-group">
               <%= f.input :contact_phone, as: :tel, label: "📞 Contact Phone" %>
            </div>

            <%= f.button :submit, "Submit Booking", class: "btn btn-primary" %>
         <% end %>
      </div>
   </div>
</div>
